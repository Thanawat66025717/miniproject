# AI Sentiment & Rating Analysis (Thai) 🇹🇭

ระบบวิเคราะห์รีวิวร้านอาหารภาษาไทยด้วย AI ทำนายคะแนนดาว (1-5), อารมณ์ (Sentiment), หมวดหมู่ร้านอาหาร, และการประชด (Sarcasm)

## ✨ ฟีเจอร์หลัก (Features)
- **Tokenization:** ใช้ `pythainlp` (engine='newmm') ตัดคำภาษาไทยแม่นยำ
- **Models (ML):**
  - **Rating:** Ridge Regression (ทำนายคะแนน 1-5 ดาว)
  - **Sentiment:** Logistic Regression (Positive / Neutral / Negative)
  - **Category:** Logistic Regression (11 หมวดหมู่)
  - **Sarcasm:** Logistic Regression (ตรวจจับการประชด)
- **Hybrid Logic:** ผสมผสาน ML กับ Rule-based keywords เพื่อความแม่นยำสูงสุด
- **UI:** หน้าเว็บสวยงามแบบ Glassmorphism (HTML/CSS/JS) รองรับ **Headline** และ **Body**
- **Deploy:** ใช้งานง่ายผ่าน Flask Server พร้อม Health Check (`/health`)

## 🛠️ วิธีการติดตั้ง (Installation)

1. **ติดตั้ง Python** (แนะนำเวอร์ชัน 3.8+)
2. **ติดตั้ง Library ที่จำเป็น:**
   ```bash
   pip install flask pandas scikit-learn pythainlp joblib requests
   ```

## 🚀 วิธีรันโปรแกรม (Usage)

1. **เปิดเซิร์ฟเวอร์:**
   ```bash
   python app.py
   ```
2. **ใช้งานผ่านเว็บ:**
   เปิด Browser ไปที่ `http://127.0.0.1:5000`

3. **(Optional) ส่งลิงก์ให้เพื่อน:**
   รันไฟล์ `share_link.bat` (จะสร้าง Public URL ให้)

## 📂 โครงสร้างไฟล์ (File Structure)
- `app.py` - โค้ดหลักของ Flask Server และ Logic ทั้งหมด
- `train_all_models.py` - สคริปต์สำหรับเทรนโมเดลทั้งหมด
- `dataset1.csv` - ข้อมูลสอน (Dataset)
- `templates/index.html` - หน้าเว็บ UI
- `*.joblib` - ไฟล์โมเดลที่เทรนเสร็จแล้ว
- `README.md` - คู่มือการใช้งาน

## 🧠 Model Logic
ระบบใช้ **Hybrid Approach**:
1. แปลงข้อความเป็นเวกเตอร์ด้วย **TF-IDF**
2. ใช้โมเดลทำนายค่าพื้นฐาน
3. ตรวจสอบ **Keywords** (เช่น "ไม่อร่อย", "เฉยๆ") เพื่อปรับคะแนน (Override)
4. คำนวณดาวแบบต่อเนื่อง (เช่น 3.5, 4.0)

---
*Developed by AI Assistant*